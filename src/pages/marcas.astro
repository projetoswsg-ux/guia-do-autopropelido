---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const todasNoticias = await getCollection('noticias');

const marcas = [
  {
    nome: 'Bashi Motors',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Bee Brasil',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Conduzza ',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Eco Motors Brasil ',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Gotrix ',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'MotoChefe',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Neon Mobilidade',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'NXT Autopropelidos',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Smartway',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Watts Mobilidade',
    pais: 'Brasil 🇧🇷',
    destaque: '',
    site: '',
  },
  {
    nome: 'Segway',
    pais: 'EUA 🇺🇲',
    destaque: '',
    site: '',
  },
  {
    nome: 'Ducati',
    pais: 'Itália 🇮🇹',
    destaque: '',
    site: '',
  },
  {
    nome: 'AIMA',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Agie Motors',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Engwe',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Foston',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Honeywhale',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Navee',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Nami Electric',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Shineray',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Tomate',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
  {
    nome: 'Xiaomi',
    pais: 'China 🇨🇳',
    destaque: '',
    site: '',
  },
];

// Ordem de exibição dos países
const ordemPaises = ['Brasil 🇧🇷', 'China 🇨🇳', 'EUA 🇺🇲', 'Itália 🇮🇹'];

// Normaliza texto (minúsculas, sem acento)
function norm(s) {
  return (s || '')
    .toString()
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '');
}

// Encontra notícias que mencionam a marca
function noticiasDaMarca(marca) {
  const termo = norm(marca.nome);
  return todasNoticias
    .filter((n) => {
      const texto = norm(`${n.data.title} ${n.data.description} ${n.body || ''}`);
      return texto.includes(termo);
    })
    .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
    .slice(0, 3);
}

// Anexa as notícias a cada marca
const marcasComNoticias = marcas.map((m) => ({
  ...m,
  noticias: noticiasDaMarca(m),
}));

// Agrupa por país
const marcasPorPais = marcasComNoticias.reduce((acc, marca) => {
  if (!acc[marca.pais]) acc[marca.pais] = [];
  acc[marca.pais].push(marca);
  return acc;
}, {});
---

<BaseLayout
  title="Marcas, Fabricantes e Distribuidores — Guia do Autopropelido"
  description="Principais marcas, fabricantes e distribuidores de autopropelidos e mobilidade elétrica geral no Brasil."
>
  <section class="marcas-page">
    <header class="marcas-header">
      <h1>Marcas, Fabricantes e Distribuidores</h1>
      <p>Principais marcas, fabricantes e distribuidores de autopropelidos e mobilidade elétrica geral no Brasil.</p>
    </header>

    {ordemPaises.map((pais) => (
      marcasPorPais[pais] && (
        <section class="marcas-pais">
          <h2 class="marcas-pais-titulo">
            <span class="marcas-pais-bandeira">{pais}</span>
          </h2>

          <div class="marcas-grid">
            {marcasPorPais[pais].map((marca) => (
              <article class="marca-card">
                <header class="marca-cabecalho">
                  <h3 class="marca-nome">{marca.nome}</h3>
                  <span class="marca-pais">{marca.pais}</span>
                </header>

                <p class="marca-destaque">{marca.destaque}</p>

                {marca.noticias.length > 0 && (
                  <div class="marca-noticias">
                    <span class="marca-noticias-titulo">Na mídia</span>
                    <ul>
                      {marca.noticias.map((n) => (
                        <li>
                          <a href={`/noticias/${n.slug}`}>
                            {n.data.title}
                          </a>
                        </li>
                      ))}
                    </ul>
                  </div>
                )}

                <a class="marca-busca" href={`/buscar?q=${encodeURIComponent(marca.nome)}`}>
                  Ver tudo sobre {marca.nome} →
                </a>
              </article>
            ))}
          </div>
        </section>
      )
    ))}
  </section>
</BaseLayout>
